{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal shell capability for the DeepSeek Harness desktop shell. The shell loads an external Host URL at runtime (Task 10); this capability grants only the core window/event defaults the shell needs and no per-webview permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
