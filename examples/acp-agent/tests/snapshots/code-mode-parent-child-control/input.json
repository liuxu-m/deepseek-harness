{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Start a controlled child, report, inspect status, steer, interrupt, close, wait, then reply CONTROL_DONE." },
    { "op": "waitForTurnEnd" }
  ]
}
